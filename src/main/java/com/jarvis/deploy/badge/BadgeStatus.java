package com.jarvis.deploy.badge;

/**
 * Represents the possible status values for a deployment badge.
 */
public enum BadgeStatus {
    SUCCESS,
    FAILED,
    IN_PROGRESS,
    PENDING,
    ROLLED_BACK,
    UNKNOWN
}
